Keystore: For Play Store/signing you need a keystore (jks). Do NOT commit real keystores publicly.
You can configure GitHub Actions secrets to provide KEYSTORE_BASE64, KEYSTORE_PASS, KEY_ALIAS, KEY_PASS for CI signing.
